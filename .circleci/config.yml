version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers

    working_directory: ~/protractorTest

    steps:
      - checkout
      
      - run: yarn install
	  - run: yarn test